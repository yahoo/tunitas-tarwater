#!/bin/sh
source ${0%/*}/rigging.sh || exit 70
exit 70
port=8081
pid=$(${exe?} --port=${port?} </dev/null &) &&
sleep 1 &&
curl localhost:${port?}/sync &&
echo OK

# be careful what you kill here.
kill ${pid:-0}
